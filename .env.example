# Site Worker: active-snapshot search API. This URL is public, not a secret.
BITCASE_INDEX_API_URL=https://bitcase-index-api.example.workers.dev

# Site Worker: optional token used only when a visitor explicitly imports and
# visualizes an exact skills.sh or GitHub SKILL.md URL.
GITHUB_SKILL_TOKEN=
