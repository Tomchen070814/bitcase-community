{
  "name": "@bitcase/index-worker",
  "version": "0.4.0-beta.12",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir ../.index-worker-dist",
    "deploy": "wrangler deploy"
  }
}
